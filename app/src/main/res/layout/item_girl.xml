<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="girl"
            type="com.lijiankun24.databindingpractice.data.Girl"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="112dp"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="112dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_girl_name"
                style="@style/TextViewStyle"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:text="@{girl.name}"
                android:paddingLeft="16dp"
                android:paddingStart="16dp"
                android:paddingTop="24dp"/>

            <TextView
                android:id="@+id/tv_girl_age"
                style="@style/TextViewStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:paddingLeft="16dp"
                android:paddingStart="16dp"
                android:text="@{girl.from}"/>
        </LinearLayout>

        <ImageView
            android:id="@+id/iv_girl_avatar"
            android:layout_width="112dp"
            android:layout_height="112dp"
            android:layout_gravity="center_vertical"
            android:contentDescription="@null"
            android:paddingBottom="16dp"
            android:paddingEnd="16dp"
            android:paddingRight="16dp"
            android:paddingTop="16dp"
            app:imageUrl="@{@string/avatar_url}"
            app:placeHolder="@{@drawable/ic_launcher}"
            app:error="@{@drawable/ic_launcher}"/>
    </LinearLayout>
</layout>

