<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View"/>

        <variable
            name="course"
            type="com.lijiankun24.databindingpractice.model.Course"/>

        <variable
            name="user"
            type="com.lijiankun24.databindingpractice.model.User"/>

        <variable
            name="observableTeacher"
            type="com.lijiankun24.databindingpractice.model.ObservableTeacher"/>

        <variable
            name="handler"
            type="com.lijiankun24.databindingpractice.main.MainClickHandlers"/>

        <variable
            name="mainPresenter"
            type="com.lijiankun24.databindingpractice.main.MainPresenter"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:context="com.lijiankun24.databindingpractice.main.MainActivity">

        <include
            android:id="@+id/include_toolbar"
            layout="@layout/view_toolbar"/>

        <android.support.v4.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_name"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:onClick="@{handler::onNameClick}"
                    android:text="@{course.name}"
                    android:textColor="@android:color/black"
                    android:textSize="16sp"/>

                <View style="@style/LineStyle"/>

                <TextView
                    android:id="@+id/tv_time"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:onClick="@{handler::onTimeClick}"
                    android:text="@{course.time}"
                    android:textColor="@android:color/black"
                    android:textSize="16sp"/>

                <View style="@style/LineStyle"/>

                <TextView
                    android:id="@+id/tv_teacher"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:onClick="@{handler::onTeacherClick}"
                    android:text="@{course.teacher}"
                    android:textColor="@android:color/black"
                    android:textSize="16sp"/>

                <View style="@style/LineStyle"/>

                <TextView
                    android:id="@+id/tv_save"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:onClick="@{(view) -> mainPresenter.saveCourse(view, course)}"
                    android:text="@string/save"
                    android:textColor="@android:color/black"
                    android:textSize="16sp"/>

                <View style="@style/LineStyle"/>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <CheckBox
                        android:id="@+id/checkbox"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:checked="@{user.isAdult}"
                        android:onCheckedChanged="@{(button, b) -> mainPresenter.changeUserIsAdult(context, user,  b)}"/>

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="40dp"
                        android:layout_gravity="center_vertical"
                        android:background="@android:color/holo_blue_light"
                        android:gravity="center"
                        android:text="@string/observable"
                        android:textColor="@android:color/black"
                        android:visibility="@{user.isAdult ? View.VISIBLE : View.INVISIBLE}"/>

                </LinearLayout>

                <include
                    layout="@layout/view_include"
                    bind:user="@{user}"/>

                <include
                    layout="@layout/view_include1"
                    bind:observableTeacher="@{observableTeacher}"/>
            </LinearLayout>
        </android.support.v4.widget.NestedScrollView>
    </LinearLayout>
</layout>