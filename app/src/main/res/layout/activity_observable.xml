<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="java.util.List"/>

        <import type="android.databinding.ObservableArrayList"/>

        <import type="android.databinding.ObservableField"/>

        <import type="java.lang.String"/>

        <import type="android.view.View"/>

        <import type="com.lijiankun24.databindingpractice.common.model.Student"/>

        <variable
            name="nameList"
            type="List&lt;String>"/>

        <variable
            name="studentList"
            type="ObservableArrayList&lt;Student>"/>

        <variable
            name="observableText"
            type="ObservableField&lt;String>"/>

        <variable
            name="observablePresenter"
            type="com.lijiankun24.databindingpractice.observable.ObservablePresenter"/>

        <variable
            name="observableTeacher"
            type="com.lijiankun24.databindingpractice.common.model.ObservableTeacher"/>

        <variable
            name="student"
            type="Student"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context="com.lijiankun24.databindingpractice.observable.ObservableActivity">

        <include
            android:id="@+id/include_toolbar"
            layout="@layout/view_toolbar"/>

        <TextView
            android:id="@+id/tv_list"
            style="@style/TextViewStyle"
            android:background="@android:color/holo_green_light"
            android:hint="@string/observable_click"
            android:onClick="@{(view) -> observablePresenter.changeText(view, nameList)}"/>

        <View style="@style/LineStyle"/>

        <TextView
            android:id="@+id/tv_string"
            style="@style/TextViewStyle"
            android:background="@android:color/holo_green_light"
            android:hint="@string/observable_click"
            android:onClick="@{(view) -> observablePresenter.changeText(studentList)}"
            android:text="@{String.format(@string/observable_list2, studentList.get(0).name, studentList.get(0).age)}"/>

        <View style="@style/LineStyle"/>

        <EditText
            android:id="@+id/et_text"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@android:color/holo_green_light"
            android:hint="@string/include_input_name"/>

        <View style="@style/LineStyle"/>

        <TextView
            android:id="@+id/tv_text"
            style="@style/TextViewStyle"
            android:background="@android:color/holo_green_light"
            android:text="@{String.format(@string/observable_teacher_name,observableText.get())}"/>

        <include
            android:id="@+id/include_view1"
            layout="@layout/view_include1"
            bind:observableTeacher="@{observableTeacher}"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <CheckBox
                android:id="@+id/checkbox"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:checked="@{student.isAdult}"/>
            <!--android:onCheckedChanged="@{(button, b) -> mainPresenter.changeUserIsAdult(context, student,  b)}"/>-->

            <TextView
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_gravity="center_vertical"
                android:background="@android:color/holo_blue_light"
                android:gravity="center"
                android:text="@string/observable"
                android:textColor="@android:color/black"
                android:visibility="@{student.isAdult ? View.VISIBLE : View.INVISIBLE}"/>

        </LinearLayout>
    </LinearLayout>
</layout>
